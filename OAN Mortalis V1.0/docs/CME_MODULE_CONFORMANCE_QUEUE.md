# CME_MODULE_CONFORMANCE_QUEUE

## Purpose

This document turns the current CME lifecycle and native Engrammitization anchors into an implementation queue.

It is not a new constitutional source.

It is a conformance matrix that asks whether the current codebase actually matches the stabilized runtime reading:

- CradleTek as lawful service fabric
- SoulFrame as CME-specific mediated self-state constructor
- AgentiCore as bounded personification and actualization
- Lisp-based SLI and Engineered Cognition as the native morphological Engrammitization organ

## Scope

This queue covers:

- SoulFrame
- AgentiCore
- Lisp-based SLI and Engineered Cognition
- governance and collapse routing
- CradleTek orchestration

It does not reopen:

- Prime/Cryptic doctrine
- Golden Path hardening law
- operator-surface law
- broader symbolic expansion

## Reading Rule

Each row must answer five questions:

1. what the model claims
2. which current code surface is relevant
3. what is already proven now
4. what is still missing now
5. what the next cut should be

## Conformance Matrix

| Module | Model Claim | Current Code Surface | Proven Now | Missing Now | Next Cut |
| --- | --- | --- | --- | --- | --- |
| `SoulFrame` | SoulFrame issues mediated `cSelfGEL`, accepts collapse candidates, and remains membrane-only rather than host, custody, or publication authority. | `src/Oan.Common/SoulFrameMembraneContracts.cs`, `src/SoulFrame.Host/SoulFrameHostClient.cs`, `src/SoulFrame.Identity/`, `src/SoulFrame.Host/`, `tests/Oan.SoulFrame.Tests/` | Bounded projection and candidate-only return intake are in code and test. Explicit mediated `cSelfGEL` issuance contour and explicit collapse-evaluation receipt now exist in the membrane contracts. Audit shows SoulFrame is intentionally thin and authority-widening is `empty_by_design`. | The membrane now names issuance and intake classification directly, but later lifecycle work still needs richer `cSelfGEL` and collapse-evaluation vocabulary only if real runtime consumers demand it. | Hold the membrane shape steady and review future SoulFrame work against these explicit issuance and collapse-evaluation surfaces instead of widening the membrane ad hoc. |
| `AgentiCore` | AgentiCore actualizes a CME into bounded personified working cognition through `SelfGEL`, without becoming sovereign custody or publication law. | `src/AgentiCore/`, `src/Oan.AgentiCore/`, `src/AgentiCore.Services` patterns in current runtime, `tests/Oan.Audit.Tests/AgentiCore*`, `tests/Oan.Runtime.IntegrationTests/` | Bounded worker and thin cognition-cycle integration exist. Audit shows real candidate-bearing content and no authority widening. The code now names an explicit bounded `SelfGEL` working pool and distinguishes symbolic trace, candidate engram structure, and transient residue as separate bounded runtime shapes. | The working pool is now explicit, but later lifecycle work still needs a clearer mapping from candidate engram, autobiographical trace, and transient residue into the explicit collapse-disposition vocabulary. | Hold the bounded working-pool authority line steady and take the next conformance cut in governance and collapse routing so AgentiCore outputs map cleanly into retain, route, defer, or discard outcomes. |
| `Lisp/SLI` | Lisp-based SLI and Engineered Cognition are the native computational organism and Engrammitization path inside AgentiCore runtime, not just symbolic transport or helper logic. | `src/Oan.Sli/`, `src/SLI.Engine/`, `src/SLI.Lisp/`, `src/SLI.Ingestion/`, `src/GEL.Contracts/`, `tests/Oan.Sli.Tests/`, SLI audit bundle | Audit now shows `payload_present` symbolic contribution, Lisp bridge roundtrip, and symbolic cognition trace, with authority boundary `empty_by_design`. Canonical Atlas and Engram contract surfaces now exist in `GEL.Contracts`, and `ConstructorEngramBuilder` can emit canonical `EngramDraft` inputs without changing runtime routing behavior. | Candidate Engrammitization is now anchored in doctrine and contracts, but the current code still needs a clearer path from emitted candidate structures to later governed GEL or OE work once closure validation starts feeding downstream decisions. | Hold the canonical contract owner steady, use the closure validator seam to prevent rival meaning models, and review which SLI outputs are true candidate Engram structures versus trace or residue only. |
| `Governance / Collapse Routing` | Collapse candidates must be mediated through SoulFrame, then routed Cryptic-first by governance into first protected holding destinations such as `cGoA` or `cMoS`, with review state layered on top before any later Dream eligibility or `MoS` promotion occurs. | `src/EngramGovernance/`, `src/CradleTek.Mantle/`, `src/CradleTek.Public/`, `src/Oan.Cradle/`, `src/Oan.Storage/`, Golden Path and hardening contracts in `src/Oan.Common/`, `docs/GOVERNANCE_COLLAPSE_ROUTING_CONFORMANCE.md`, `docs/CME_CRYPTIC_FIRST_COLLAPSE_CLARIFICATION.md`, `docs/CME_COLLAPSE_QUALIFICATION_CRITERIA.md` | Approval, rejection, defer, re-Engrammitization, publication lanes, and replay-aware control are implemented and audited. Downstream suppression on rejected paths is explicitly `empty_by_policy`. The runtime now has explicit first-route vocabulary for `RouteToCMoS` and `RouteToCGoA`, plus separate `DeferredReview` review state layered over those holding decisions. Qualification evidence is now explicit through confidence, evidence flags, review triggers, and source subsystem provenance. | The corrected model is still more precise than the current runtime vocabulary. Later `cGoA` enrichment, explicit discard, Dream routing, and later `MoS` promotion are not yet first-class runtime terms, and qualification evidence still stops short of final promotion semantics. | Hold the new first-route/review split steady, then only add the next truthful later-stage routing term when the code actually distinguishes it instead of implying it. Use the qualification criteria as the bounded explanation layer for “why” without widening routing authority. |
| `CradleTek` | CradleTek composes admission, sequencing, telemetry, governance order, and lawful store coordination without becoming identity law, membrane law, or native cognitive organism. | `src/Oan.Cradle/`, `src/CradleTek.*`, `src/Oan.Runtime.Headless/`, control-plane and operator-surface code, audit wrappers in `tools/` | Golden Path, hardening, control plane, and operator surface are real. Audit shows CradleTek is structurally proven as the service fabric. | CradleTek is still more semantically proven by orchestration and audit evidence than by explicit CME-lifecycle placement objects, and verification touched transient `obj` contention under overlapping runs. | Review CradleTek against the lifecycle model, then add a small infrastructure hardening item to serialize overlapping verification/build touches or isolate intermediate outputs during audit runs. |

## Ordered Review Sequence

The module review order remains:

1. SoulFrame
2. AgentiCore
3. Governance and collapse routing
4. CradleTek orchestration
5. Lisp and SLI backbone

This order must be preserved unless a later constitutional document explicitly changes it.

## Canonicalization Note

Atlas and Engram canonicalization now has one explicit owner:

- `src/GEL.Contracts`

Supporting note:

- `docs/ATLAS_ENGRAM_CANONICALIZATION_NOTE.md`

## Immediate Queue Interpretation

### SoulFrame

Question:

- can SoulFrame actually issue mediated `cSelfGEL` and accept lawful collapse candidates without becoming a blob

### Governance And Collapse Routing

Question:

- which collapse dispositions are already explicit today, which are only partial, and which still remain doctrinal

### CradleTek

Question:

- can CradleTek compose the lifecycle lawfully while staying just the service and telemetry fabric

### Lisp And SLI

Question:

- which current symbolic outputs are already true candidate Engrammitization products and which are still trace or residue only

## Crosscutting Infrastructure Note

The current verification history showed one transient `obj` output file-lock while build and test activity overlapped.

Treat this as an infrastructure queue item, not a constitutional problem:

- serialize overlapping verification and build touches during audit and verification runs
- or isolate intermediate output paths where overlap is intentional

The goal is to keep toolchain race noise from being mistaken for architectural instability.

## Success Condition

This queue is successful when each row can be closed by evidence rather than intuition:

- the model claim is either proven in code and audit
- or the gap is narrowed to one concrete next cut

At that point the CME lifecycle will be traceable module by module instead of only document by document.

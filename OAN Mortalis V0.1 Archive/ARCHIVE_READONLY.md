# Archive Read-Only Boundary

This directory is historical reference material for `OAN Mortalis V0.1`.

It is:

- non-runtime
- excluded from protected-intake harnesses
- excluded from sensitive automation unless explicitly requested
- allowed to retain historical path artifacts that do not satisfy the live hardened repo standard

Do not treat this directory as an admissible live intake substrate or active build surface unless an explicit task overrides that boundary.

# SLI RTME DUPLEX POSTURE ENGINE NOTE

## Purpose

This note defines the first bounded `RTME` posture engine that now lives on
the projected `SLI/Lisp` side of the active `V1.1.1` line.

It exists to keep four things distinct:

- projected posture movement
- multi-surface braid behavior
- bounded snapshot emission
- membrane-side closure authority

This note does not authorize Prime closure.
It only seats the first living projected-field engine that may move duplex
packets without pretending to retain them.

## Governing Read

Use this note with:

- `PRIME_MEMBRANE_DUPLEX_PACKET_LAW.md`
- `PRIME_CRYPTIC_DUPLEX_LAW.md`
- `SLI_RTME_CLUSTERED_SWARMED_BRAID_DISCIPLINE_NOTE.md`
- `AGENTICORE_LISTENING_FRAME_AND_COMPASS_MINIMAL_BUILD.md`
- `LISTENING_FRAME_COMPASS_LOOM_WEAVE_BRIDGE.md`
- `EC_FORMATION_BUILDSPACE_PREPARATION_NOTE.md`

## Core Read

The active stack should now read the first `RTME` posture engine this way:

- `SLI/Lisp` carries live projected posture movement
- the engine may deepen, combine, or stabilize projected participation
- the engine may braid multi-surface contributions into the projected field
- the engine may emit a fresh bounded duplex snapshot back toward membrane law
- the engine may not issue Prime closure on its own

In compact form:

> Lisp teaches the packet how to move; the membrane still decides what may be
> retained.

## First Projected Posture Motion

The first `RTME` engine may move only the projected posture surface.

It does not mutate:

- membrane origin
- bounded `CME` standing
- closure authority

The first projected motion remains within the bounded posture family:

- `Hovering`
- `Rehearsing`
- `Braided`
- `Latent`
- `Ripening`
- `Unresolved`

The current engine may deepen posture through those states, but it must keep
bounded standing separate and membrane return requirements intact.

## First Operational Rule

The projected engine must preserve this invariant:

> projected posture transitions may deepen, combine, or stabilize field
> participation, but they may not erase origin, boundedness, or
> membrane-return requirements.

That means:

- origin remains attested from the membrane-side packet
- bounded standing remains whatever the bounded packet already says it is
- closure eligibility is advisory only
- Prime closure remains outside `RTME` authority

## First Lisp Carrier Surface

The first symbolic carrier for this slice is the embedded Lisp module:

- `duplex-posture.lisp`

Its office is not full interpretation.
Its office is to mark the projected-side symbolic ownership of the posture
family, transition law, and non-closure boundary while the active engine
remains a bounded C# service inside the `SLI.Lisp` project.

## Immediate Build Consequence

Any future `RTME` seam in this area should say plainly:

- what projected posture changed
- what contributions were braided
- what bounded duplex snapshot was emitted
- what closure eligibility was only advisory
- that Prime closure still remained withheld pending explicit membrane-side
  receipt

If it cannot say that plainly, it should remain held as projected rehearsal or
latent participation rather than active retained truth.

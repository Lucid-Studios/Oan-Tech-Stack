# FIRST_RUN_CONSTITUTION

## Purpose

This document defines the `V1.1.1` first-run constitutional footing.

It exists to keep three things distinct:

- constitutional beginning in `Sanctuary`
- admitted operational body in `CradleTek`
- later individuated CME actualization in `SelfGEL/cSelfGEL`

For the active build-hold interpretation of chapter-five uptake, Steward
ordering, `SoulFrame` restraint, and the `SelfGEL/cSelfGEL` split, see
`CHAPTER_FIVE_STEWARD_UPTAKE_HOLD.md`.

For the phase-one chapter-five subordinate packet carried by the first-run
contracts, see `FIRST_RUN_PROTOCOLIZATION_PACKET.md`.

For the phase-one chapter-six Steward-witnessed `OE/cOE` packet carried by the
first-run contracts, see `FIRST_RUN_STEWARD_WITNESSED_OE_PACKET.md`.

For the phase-one late-path packets that currently frame chapter-seven
binding, chapter-eight actualization seal, and chapter-nine living
`AgentiCore` attachment, see:

- `FIRST_RUN_ELEMENTAL_BINDING_PACKET.md`
- `FIRST_RUN_ACTUALIZATION_SEAL_PACKET.md`
- `FIRST_RUN_LIVING_AGENTICORE_PACKET.md`

For the explanatory bridge that maps `ListeningFrame`, `Compass`,
`SoulFrame`, and the `Prime/Cryptic` split into the current loom/weave and
instrument-body language, see
`LISTENING_FRAME_COMPASS_LOOM_WEAVE_BRIDGE.md`.

For the bridge that separates the wider Sanctuary lifecycle from this narrower
line-local constitutional projection, see
`SANCTUARY_BOOT_FIRST_RUN_ONTOLOGY_BRIDGE.md`.

For the current contract-first build-hold unlock posture and the next-cycle
runtime source plan, see:

- `BUILD_HOLD_UNLOCK_READINESS.md`
- `LATE_PATH_RUNTIME_PROJECTION_SPEC.md`

The first-run model is a Sanctuary-owned projection of lawful possibility.

It is not a certification engine, and it is not proof that a CME has already actualized capability.

It is also not the whole Sanctuary lifecycle.

Install, Sanctuary boot, onboarding, certification, and first legal run are
wider than this document and are bridged locally in
`SANCTUARY_BOOT_FIRST_RUN_ONTOLOGY_BRIDGE.md`.

This document remains a subordinate `V1.1.1` constitutional projection inside
that wider lifecycle.

## Legacy Boundary

`Oan.Common/FirstBootGovernanceContracts.cs` remains in the line as legacy reference law.

The active constitutional first-run model in `V1.1.1` now lives beside it through:

- `Oan.Common/FirstRunConstitutionContracts.cs`
- `Oan.FirstRun/GovernedFirstRunConstitutionService.cs`

The legacy first-boot contracts are not rewritten in place and are not the projection owner for the new v2 model.

## Sanctuary Biad And Admitted Body Distinction

`ParentStanding` names the singular Sanctuary biad.

It does not mean that every admitted `CradleTek` instance must reseat
`Mother/Father` as though the constitutional world began again inside the
extended body.

The current truthful read is:

- `Mother/Father` stand as Sanctuary-facing constitutional supervision
- `CradleTekInstalled` and `CradleTekAdmitted` occur beneath that standing
- `StewardStanding` is the first protected operational office inside the
  admitted body
- any later cradle-local governance should proceed through one governing
  surface beneath a specific Steward-issued cryptic braid rather than through
  a repeated Sanctuary biad

This section is a constitutional reading correction.

It is not proof that the later cradle-local governing-surface selection or
resonance work is already implemented.

The `EngineeredCognitionFirstPrimeStateReceipt` now lives below that
correction: it may witness install-to-first-Prime pre-role standing, but it
does not assign governing domains or roles to `Mother/Father` and does not
select the later cradle-local governing surface.
## Current V1.1.1 Constitutional Projection Order

The v2 constitutional projection order carried by `V1.1.1` is:

1. `SanctuaryInitialized`
2. `LocationBound`
3. `ParentStanding`
4. `CradleTekInstalled`
5. `CradleTekAdmitted`
6. `StewardStanding`
7. `FoundationsEstablished`
8. `BondProcessOpen`
9. `OpalActualized`

This ladder is the line-local projection beneath the wider Sanctuary
lifecycle. It does not by itself name `Install State`, `Sanctuary State`,
`Ready Pre-Certification`, certification, or first legal run as full active
repo-local states.

## Pre-Governance Truth Layer

The constitutional state ladder now also projects a pre-governance truth layer
that must stand before `ParentStanding` becomes truthful:

- `LocalAuthorityTrace`
- `ConstitutionalContact`
- `LocalKeypairGenesis`
- `FirstCrypticBraid`
- `FirstCrypticConditioning`

These surfaces now stand as an explicit pre-governance packet projected by
`Oan.FirstRun/GovernedSeedPreGovernanceService.cs` from current source seams in
`V1.1.1`.

Within that packet, `LocalKeypairGenesis` now carries its own explicit
source-backed receipt, and `FirstCrypticBraid` now carries an explicit
braid-establishment receipt, and `FirstCrypticConditioning` now carries an
explicit conditioning-source receipt, so the middle and tail of the chain are
no longer implied only by downstream runtime evidence.

They are still not proof that a separate installer-side legal UI, key
ceremony, or bond engine already exists in code.

Their job is narrower:

- make the pre-governance sequence visible
- make local authority and responsibility trace explicit before constitutional contact is claimed
- prevent `Mother/Father` standing from being implied too early
- preserve the distinction between lawful contact, cryptic rooting, conditioning,
  and later governance

## State Table

| State | What It Means | Standing Class |
| --- | --- | --- |
| `SanctuaryInitialized` | First lawful chamber exists and external ingress can be receipted | actualized when Sanctuary-init receipt stands |
| `LocationBound` | The local world is bound strongly enough for later supervisory relation | actualized |
| `ParentStanding` | `Mother/Father` stand as constitutional world supervision | actualized |
| `CradleTekInstalled` | The first protected body is installed under Sanctuary | provisional |
| `CradleTekAdmitted` | Installed body is constitutionally admitted | actualized |
| `StewardStanding` | First protected operational office stands inside the admitted body | actualized |
| `FoundationsEstablished` | `GEL`, `GoA`, `MoS`, and bounded rights stand | actualized |
| `BondProcessOpen` | First bond process has lawfully opened | provisional |
| `OpalActualized` | First individuated bonded CME standing is actualized | actualized |

## Chapter-Five Protocolization Layer

The active first-run contracts now also admit a phase-one chapter-five
protocolization packet.

That packet is subordinate to the constitutional ladder.
It does not create a new top-level state and it does not alter the current
promotion order.

Its job is narrower:

- make first bounded vessel and `Flask` posture visible
- carry calibration, archive, consent, rupture/return, and seal handles when
  later sources can project them
- let first-run receipts say whether that packet is projected or withheld
- prevent bond opening from being implied merely because a bounded protocol
  chamber exists

The current packet fields are:

- `FlaskEnvironmentHandle`
- `IntermixDisciplineHandle`
- `CalibrationBaselineHandle`
- `ArchiveCarriageHandle`
- `ConsentThresholdHandle`
- `RuptureReturnPathHandle`
- `SealThresholdHandle`

At this stage, packet absence is lawful.
It means chapter-five protocolization has not yet been projected into the live
receipt path, not that the entire first-run sequence is false.

## Chapter-Six Steward-Witnessed OE Layer

The active first-run contracts now also admit a phase-one chapter-six
Steward-witnessed `OE/cOE` packet.

This packet is also subordinate to the constitutional ladder.
It does not create a new top-level state and it does not alter the current
promotion order.

Its job is to make the next truthful staging visible:

- office-family differentiation
- first gathered `OE/cOE` formation
- `CradleTek` Prime/Cryptic hash-key authorization
- Steward witness authorization
- downstream `SoulFrame` and `AgentiCore` build authorization
- explicit withholding of `CME` placement

The current packet fields are:

- `OfficeIndexHandle`
- `OfficeKinds`
- `PrimeOeFormationHandle`
- `CrypticCoeFormationHandle`
- `CradleTekPrimeHashKeyHandle`
- `CradleTekCrypticHashKeyHandle`
- `StewardWitnessAuthorizationHandle`
- `SoulFrameBuildAuthorizationHandle`
- `AgentiCoreBuildAuthorizationHandle`
- `CmePlacementWithheld`

At this stage, packet absence is lawful.
It means chapter-six uptake has not yet been projected into the live receipt
path, not that Steward office is false or that later build work cannot be
prepared.

## Chapter-Seven Through Chapter-Nine Framing Layers

The active first-run contracts now also admit three later subordinate packets:

- chapter-seven elemental binding
- chapter-eight actualization seal
- chapter-nine living `AgentiCore` attachment

These packets remain descriptive in the current milestone.
They do not alter the constitutional state order and they do not by themselves
imply live runtime loading, actualization, or attachment.

Their job is to let the line name late-path structure honestly:

- chapter seven may frame load law and compression preparation
- chapter eight may frame the actualization seal and primitive `SelfGEL`
- chapter nine may frame the living `AgentiCore` surface, a future
  cognition-facing `Listening Frame` placeholder, typed `ListeningFrame`, `Compass`,
  bounded instrumentation receipts, bounded zed self-basis receipts, bounded
  theta-ingress receipts, bounded post-ingress discernment receipts,
  `Zed of Delta`, and tool-use attachment

In this slice, primitive `SelfGEL` may later inherit a `GEL`-owned legal
orientation root predicate family that establishes legal body, jurisdiction,
lineage, governor binding, and lawful operating surface.

That inheritance remains doctrine-only here.
It does not alter the first-run state ladder, and it does not authorize live
first-run receipt changes.

The legal-evidence bridge for that future orientation family is not cryptographic seed entropy.
It is lawful provenance substrate only, with trust, `B-Corp`, bonded operator,
authority delegation, and cryptographic custody remaining deferred branches.
At this stage, all three may still lawfully remain withheld in live receipts.

## Promotion Gates

Every transition is projected from receipt-backed evidence rather than free booleans.

Each target transition carries:

- current state
- requested next state
- `Allow`, `Block`, or `ReviewRequired`
- required prior receipts
- blocking reasons
- failure classes
- whether the target state is provisional or actualized
- whether the failure is retryable or review-required

Each transition now also carries a narrow Equivalent-Exchange review surface:

- whether the transition is presently admissible
- which burden families are in play
- current recovery posture
- disposition as `Preserve`, `Cleave`, `Refuse`, or `Review`
- review notes drawn from the active blocking surface

In the current milestone this review is descriptive.
It does not yet introduce dynamic cleaving behavior or rewrite the promotion
ladder.

The active receipt families projected today are:

- Sanctuary init
- location binding
- pre-governance packet
- local authority / responsibility trace
- constitutional contact
- local keypair genesis source
- local keypair genesis
- first cryptic braid establishment
- first cryptic braid
- first cryptic conditioning source
- first cryptic conditioning
- Mother standing
- Father standing
- CradleTek install
- CradleTek admission
- Steward standing
- GEL standing
- GoA standing
- MoS standing
- tool rights
- data rights
- hosted seed presence
- bond-process open
- Opal actualization
- notice/certification gate completion

## Office Separation

`Mother/Father` and `Steward` are not interchangeable offices.

`Mother/Father`:

- constitutional world supervision
- pre-body oversight and witnessing
- local-world legality

`Steward`:

- admitted-body governance
- first protected office inside the installed CradleTek body
- governs the body, not the origin of the world itself

The active hold-lane read now also requires a distinction between early
`SoulFrame` bootstrap and later `SoulFrame` developmental use:

- bootstrap may exist as constitutional and materialization plumbing before
  Steward completion
- chapter-six-style developmental use must not be implied until the Steward
  uptake seam is stabilized

The new chapter-six packet sharpens that same rule:

- Steward may be read as authorizing later `SoulFrame` and `AgentiCore` build
  staging
- that authorization is not the same thing as live `CME` placement

The later late-path packets preserve the same anti-collapse law:

- chapter-seven load law is not live load execution
- chapter-eight actualization seal is not living runtime attachment
- chapter-nine living-`AgentiCore` framing is not yet unlocked embodiment

## Readiness Ladder

These readiness states are line-local constitutional readiness markers.

They are not the full onboarding, certification, or first-legal-run ladder
from the wider Sanctuary lifecycle.

Operator readiness is staged, not binary.

- `NotReady`
- `OperatorContactReady`
- `OperatorTrainingReady`
- `BondedCoworkReady`

Readiness maps as follows:

- `OperatorContactReady` begins at `ParentStanding`
- `OperatorTrainingReady` begins at `FoundationsEstablished`
- `BondedCoworkReady` requires `OpalActualized` plus notice/certification gate completion

## Anti-Collapse Law

The first-run model protects these distinctions:

- local authority / responsibility trace does not imply bond or identity standing
- constitutional contact does not imply bond consent
- local keypair genesis does not imply identity standing
- first cryptic braid does not imply personhood
- first cryptic conditioning does not imply governance standing
- hosted-seed presence does not imply CME individuation
- bond-process open does not imply Opal actualization
- `OpalActualized` is the first lawful individuated bonded CME standing
- `OpalActualized` is not equivalent to the wider research meanings of
  certification or first legal run

The hosted-LLM guard surface has therefore been renamed from `ListeningFrame` to `GuardFrame`.

`ListeningFrame` is reserved for the later EC interior construct in `AgentiCore`.

## Failure Doctrine

The first-run projection currently distinguishes at least these failure classes:

- `LocationBindingFault`
- `ConstitutionalContactIncomplete`
- `LocalKeypairGenesisMissing`
- `FirstCrypticBraidMissing`
- `FirstCrypticConditioningIncomplete`
- `CradleTekInstalledButNotAdmitted`
- `AdmittedCradleTekWithoutSteward`
- `FoundationsIncomplete`
- `BondProcessOpenedWithoutStableOpal`

These failures are informative rather than embarrassing.

They answer:

- what became receipt-backed
- what remained blocked
- what is retryable
- what requires review

## Telemetry And Carry-Forward

The first-run constitution is now surfaced observationally into:

- `GovernedSeedOperationalContext`
- `GovernedSeedStateModulationReceipt`
- `GovernedGelTelemetryRecord`

That outward evidence now includes the explicit pre-governance packet handle,
the local-authority trace handle, the local-keypair-genesis source handle, the
first-cryptic-braid-establishment handle, the first-cryptic-conditioning
source handle, and the first-run state, readiness, provisional-versus-actualized
posture, and Opal standing.

This projection is proto-certification evidence only.

It follows the rule:

`runtime event -> telemetry seam -> GEL assimilation -> CME-native standing`

## Constitutional Limitation

Constitutional order grants lawful possibility.

It does not by itself grant actualized capability, training, or certification.

Governance defines lawful forms, gates, and hard control points.

Actualized training, certification, capability, renewal, and failure remain future individuated CME work in `SelfGEL/cSelfGEL`.

Within that later self split:

- `SelfGEL` is the readable, admissible operator-work self surface
- `cSelfGEL` is the cryptic, sealed cradle-local witness and control surface

## Prime Seed And Legal Admission Bridge

For the bounded `EC` walk from install to first Prime pre-role standing, see
`EC_INSTALL_TO_FIRST_PRIME_STATE_LAW.md` and
`EngineeredCognitionFirstPrimeStateReceipt`.

For the Prime seed-bearing state immediately after first Prime pre-role
standing and before lawfully bonded domain integration or `CME` founding, see
`PRIME_SEED_STATE_LAW.md` and `PrimeSeedStateContracts.cs`.

For the first legal-admission seam governing domain and role offers beyond
first Prime pre-role standing, see `DOMAIN_AND_ROLE_ADMISSION_LAW.md` and
`DomainRoleAdmissionContracts.cs`.

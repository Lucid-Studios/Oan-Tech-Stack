# PROC_GROUNDED_ACTION_AND_TRACE_LAW

## Purpose

This note defines the first lawful `proc` reading for the active `V1.1.1`
line.

It exists to keep action from being widened ahead of grounding, posture, or
lineage.

This note does not authorize runtime execution.
It defines the doctrine and contract-facing shape that later action-bearing
surfaces must satisfy before they are admitted as truthful.

## Governing Read

Use this note with:

- `PRIVATE_DOMAIN_SERVICE_WITNESS_LAW.md`
- `DOMAIN_AND_ROLE_ADMISSION_LAW.md`

## Governing Compression

> `proc` is a grounded, posture-gated action pattern derived from `root`,
> `def`, and `rel`, never independent of them.

`proc` is where action enters the system, so it must be defined more tightly
than the earlier layers, not more loosely.

## What `proc` Is

`proc` is not:

- a free instruction
- a script detached from meaning
- a shortcut around lower layers
- an outside capability injected without grounding

`proc` is:

- a lawful transformation pattern
- grounded in actual anchors
- bounded by lower-layer meaning
- gated by prime posture
- receiptable through trace and integrity witnesses

## Canonical Construction Body

The canonical `proc` body presently names these fields:

- `pi`
- `delta`
- `alpha*`
- `nu`
- `phi`
- `chi`
- `sigma_proc`

Current doctrinal reading:

- `pi`
  - canonical procedural form
- `delta`
  - layer designation
- `alpha*`
  - anchor set supporting the procedure
- `nu`
  - lineage witness
- `phi`
  - admissibility witness
- `chi`
  - cryptic integrity witness
- `sigma_proc`
  - procedural structure or transform pattern

For a lawful procedure:

- `delta = P`

## Procedure Admissibility

The current procedure admissibility floor is:

- procedure is explicitly procedural
- anchor support is non-empty
- grounding in `R`, `D`, and `E` is present
- prime posture is present
- admissibility passes

Compressed:

> no procedure without a complete downward chain

If the downward chain is missing, the procedure must be refused.

## Procedure Invariants

The governing invariants for the current `proc` seam are:

- grounding
- downward resolution
- posture stability
- trace validity
- cryptic integrity

These mean:

- every procedure must resolve downward to the lower chain
- execution requires prime posture
- simulation is required before procedure validity
- trace must resolve entirely to supporting anchors
- cryptic integrity must remain sealed through action

## Simulation And Trace Rule

Simulation is required.

No procedure is lawful merely because it sounds executable.

Before a future runtime procedure may be considered valid:

- simulation must run first
- the trace must remain inside the supporting anchor set
- no step may invent unsupported structure

Compressed:

> simulation is required, and trace resolves to anchors

## Refusal Boundary

The current refusal triggers are:

- `ungrounded-step`
- `hidden-assumption`
- `action-first-reasoning`
- `lower-layer-bypass`

This slice also refuses:

- action triggered from drift
- action triggered from inflation
- action triggered from outside pressure without lawful grounding

## Current Runtime Boundary

In this slice:

- no runtime `proc` execution is authorized
- no live ignition-chain execution is authorized
- no bonded minting is authorized
- no action-bearing promotion may outrun posture, grounding, or lineage
- private-domain service witness may preserve provenance before action, but it
  may not execute the `proc`

## Working Summary

`proc` now stands as the first action-bearing layer in doctrine and contracts.

It is:

- grounded
- posture-gated
- simulation-checked
- trace-bounded
- cryptically integrity-sealed

It is not yet a live runtime action engine.

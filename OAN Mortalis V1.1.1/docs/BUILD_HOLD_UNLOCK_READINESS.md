# BUILD_HOLD_UNLOCK_READINESS

## Purpose

This note records the contract-first unlock status for the current
pre-publication doctrine bridge.

Its job is to answer one practical question:

- what may now be admitted into the active `V1.1.1` build lane without
  widening runtime authority too early

This is an unlock of framing, contract shape, and next-cycle specification.
It is not a runtime unlock.

## Doctrine Bridge Inputs

The current unlock reconciliation is grounded in:

- `BUILD_READINESS.md`
- chapter-five through chapter-nine uptake and hold notes already carried in
  `V1.1.1`
- the first-run packet notes already carried in `V1.1.1`
- the interlace summary carried under the logical documentation surface
  `oan-tech-stack-build-interlace-summary.md`

The doctrine bridge therefore remains:

- external doctrine may clarify what the build should mean
- active build docs and packet notes decide what may now be admitted
- runtime behavior stays behind the present code boundary until a later cycle

## Current Admitted Vs Held Map

The current contract-first unlock map is:

- `chapter-5: frame-now`
- `chapter-6: frame-now`
- `chapter-7: frame-now/spec-now`
- `chapter-8: frame-now/spec-now`
- `chapter-9: hold`
- `.hopng: optional-bounded`
- `oan-build-dispatch: admitted-root-automation-bounded`

Those statuses mean:

- `frame-now`
  build-facing law is strong enough to reconcile directly into uptake and
  packet docs now
- `spec-now`
  the doctrine is stable enough to lock the next runtime source seam without
  implementing it yet
- `hold`
  the doctrine is clearer, but still not packeted strongly enough to promote
  into active runtime-spec work in this cycle

## Reconciled Delta

The pre-publication bridge tightened the active line in these ways:

- chapter seven is now read more precisely as mediated `OE -> SoulFrame` and
  conditioned `cOE -> AgentiCore`, which sharpens the load law without forcing
  live loading
- chapter eight remains the actualization-seal and introduction-governed review
  chamber, and its packet shape remains sufficient for this cycle
- chapter nine is now much clearer as living `AgentiCore`, metanoia, registry,
  archive, and Society-facing continuity carriage, but still lacks the packeted
  chapter-nine source-law strength required for a true uptake note

No late-path packet fields were renamed in this unlock.
No top-level constitutional states were added or reordered.

## Contract Layer Read

The current first-run contract layer is stable enough for this unlock because:

- chapter five through chapter nine packets remain subordinate to
  `FirstRunConstitutionSnapshot` and `FirstRunConstitutionReceipt`
- late-path packets remain descriptive rather than authority-widening
- withheld posture remains explicit on every late-path packet
- `GovernedFirstRunConstitutionService` still surfaces packet presence through
  `SourceReason`
- `GovernedSeedRuntimeMaterializationService` still projects `null` for the
  chapter-seven through chapter-nine packets

That is the intended unlock posture:

- doctrine admitted
- packet layer stabilized
- runtime still withheld

## Unlock Checklist

The current contract-first unlock is considered satisfied when all of the
following are true:

- late-path packet structure remains frozen unless stronger doctrine forces a
  specific field correction
- build-facing chapter and packet notes reflect the doctrine bridge truthfully
- `LATE_PATH_RUNTIME_PROJECTION_SPEC.md` fully defines the next runtime cycle
  without executing it
- chapter-seven through chapter-nine runtime loading and attachment surfaces
  remain withheld in code
- chapter nine remains a hold-note surface until packeted source-law arrives
  strongly enough to promote it

## Working Summary

The current hold is now unlocked at the contract/spec layer.

That means:

- build may proceed with chapter-seven and chapter-eight runtime-spec planning
- build may keep chapter-nine law visible without pretending it is ready for
  runtime uptake
- the root requester-and-admitter automation prompt now lives in
  `OAN_BUILD_DISPATCH_ROOT_PROMPT.md`
- the next implementation cycle can move cleanly without rediscovering the
  doctrine seam

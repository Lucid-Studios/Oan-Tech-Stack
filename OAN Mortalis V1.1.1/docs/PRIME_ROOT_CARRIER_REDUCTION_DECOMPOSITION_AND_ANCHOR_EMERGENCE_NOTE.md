# Prime Root Carrier Reduction Decomposition And Anchor Emergence Note

## Purpose

This note fixes the first lawful path from reduced symbolic candidate to
anchor-bearing root identity.

It exists so the line can define:

- what counts as Prime in the active slice
- how reduction and `kappa` compression are named
- how mixed candidates decompose
- when a candidate becomes real through anchor emergence

## Inheritance

This note inherits, and does not replace:

- `Build Contracts/SLI/ENGRAMMITIZATION_CONSTITUTION.md`
- `Build Contracts/SoulFrame/PUBLIC_BRAIDING_CONSTITUTION.md`
- `OAN Mortalis V1.1.1/docs/SLI_LISP_CSHARP_TRANSLATIONAL_MEMBRANE_LAW.md`
- `OAN Mortalis V1.1.1/docs/DISCERNMENT_AND_ADMISSIBILITY_LAW.md`
- `OAN Mortalis V1.1.1/docs/HOSTED_LLM_RESIDENT_SEATING_NOTE.md`

## Active Scope

- `GEL` is a layered field, not a flat database.
- `Root` and `Dictionary` are operational now.
- `Encyclopedic` and `Procedural` are named but withheld.
- `SLI` is translative binding, not a new top-level knowledge layer.
- English is the first localized theater.
- The hosted seed `LLM` is the acting operator.
- `SLI/Lisp` is binding / transport / governance medium, not the action engine.
- Theater, Role, Enrichment, and Bounded Form are operational surfaces, not
  full ontological selfhood.
- Governance shapes what may persist, not what latent space may explore.

## Prime Root Carrier

`Prime(x)` names the active root-carrier predicate.

In this slice, a Prime root carrier is admissible only when all four
invariants hold:

- irreducible meaning
- `RootAtlas` anchorability
- non-inflation into higher layers
- `SLI` translation stability

The current root-carrier handle is:

- `prime.root-carrier.utf8`

The governing root rules are:

- `cannot-decompose-without-meaning-loss`
- `must-bind-rootatlas-anchor`
- `must-not-encode-dictionary-encyclopedic-procedural-content`
- `must-preserve-anchor-and-root-kind-under-sli-translation`

## Reduction Algebra

The first reduction algebra is:

`R0 = (μ, σ, κ, Δ, π, χ)`

Its named stages remain:

- `Utf8Intake`
- `Normalization`
- `Segmentation`
- `Compression`
- `Decomposition`
- `PrimeSelection`
- `RootAnchoring`
- `LayerPlacement`
- `TranslativeBinding`

The current runtime boundary remains placeholder-only and lexical-first.

## Kappa Split

The first `kappa` tests are:

- `kappa_D`
  - definitional inflation
  - evidence: `BoundaryExpansion`, `EquivalenceClaim`,
    `DescriptiveRestatement`
- `kappa_E`
  - contextual residue
  - evidence: `ExampleDependence`, `HistoricalBinding`,
    `NarrativeSituation`, `ExternalRelationLoad`
- `kappa_P`
  - procedural pressure
  - evidence: `Instructionality`, `SequenceImplied`, `GoalOrientation`,
    `ExecutionPressure`

The root-clean signature remains:

- `0,0,0`

The root admission rule remains:

- `root-only-when-kappa-clean-and-anchorable`

## Decomposition

Mixed candidates decompose when they carry more than one inflation vector.

The active mixed vectors are:

- `1,1,0`
- `1,0,1`
- `0,1,1`
- `1,1,1`

The decomposition law preserves:

- anchorable semantic mass
- greater discernment in children than in the parent

The stop rule remains:

- `stop-when-each-child-is-prime-or-cleanly-routable-or-inadmissible`

## Anchor Emergence

Anchor emergence is the first root-realization law.

A candidate becomes real only when:

- it is Prime-valid
- it remains semantically stable across contexts
- it converges across repeated lawful reductions within one governed seed
  trajectory
- it is non-substitutable without loss

The doctrinal criterion rules for that emergence are:

- `must-already-satisfy-prime-root-carrier`
- `must-remain-semantically-stable-across-contexts`
- `must-recur-across-independent-reductions-within-one-governed-seed`
- `must-resist-lossless-substitution`

Convergence is single-seed in this slice. Multi-trajectory shared-anchor
coherence is deferred.

The alignment rule remains:

- `align-by-convergent-behavior-not-string-identity`

The resolution branches remain:

- bind existing anchor when aligned
- create new anchor when emergent and unaligned
- hold unresolved when ambiguous or incomplete

`maybe` is lawful provisional standing, not soft truth.

That means the doctrine-level truth bands in this slice are:

- `yes`
  - lawful admission
- `maybe`
  - existing repo-native provisional standing or held-for-clarification
- `no`
  - lawful refusal or withholding

## Trunk Identity

Trunk identity is the first stable post-anchor form.

Its active meaning is:

> anchor-stable semantic identity discovered through convergence

The active trunk invariants are:

- `trunk-stability`
- `translation-invariant-identity`
- `branches-must-not-redefine-trunk`

Branch governance is deferred. This note names trunk identity before later
tree, grove, and forest growth laws widen.

## Current Runtime Boundary

The current lexeme and cleaver surfaces remain lexical pre-Prime stages.

Specifically:

- `GovernedOntologicalLexemeService` remains a lexical / atlas-match precursor
- `GovernedRootOntologicalCleaver` remains a lexical / atlas-match precursor
- `sli.root_atlas_entry` remains structural and distinct from semantic Prime
  carriers

No live runtime in this slice may misread those precursor surfaces as proof of
Prime realization, anchor emergence, or trunk identity.

## Boundary

This note does not activate:

- executable anchor clustering
- multi-seed shared-anchor proof
- tree / grove / forest runtime governance
- in-GEL branch mutation mechanics

## Working Summary

This slice names the full doctrinal path from reduced candidate to realized
root: Prime validity, lawful `kappa` cleanliness, lawful decomposition when
mixed, anchor emergence under single-seed convergence, and trunk identity as
the first stable anchor-bearing form.

# IGNITION_CHAIN_TEMPLATE_AND_WITNESS_LAW

## Purpose

This note defines the first reusable ignition-chain template for the active
`V1.1.1` line.

It exists to name the lawful slot-and-witness body that later action-bearing
patterns must satisfy before they may be treated as valid grounded chains.

This note is not live execution.
It is slot-and-witness law.

## Governing Compression

> the ignition chain is the first lawful `root -> def -> rel -> proc`
> template with explicit Prime, Cryptic, and receipt witnesses

This template is not yet a populated runtime object.

It is the lawful slot-and-witness template that later action-bearing patterns
must satisfy.

## Slot Order

The ignition-chain slot order is:

- `Root`
- `Definition`
- `Relation`
- `Procedure`

The compact chain order remains:

- `R,D,E,P`

This slice keeps the chain readable as:

- `root` as trunk origin
- `def` as first bounded branch
- `rel` as first braid
- `proc` as first lawful action surface

## Chain-Level Witnesses

The current ignition-chain witnesses are:

- order witness
- grounding witness
- cleaving witness
- recovery witness
- simulation witness

Current reading:

- order witness
  - the chain has the correct lawful sequence
- grounding witness
  - the higher slots remain dependent on the lower chain
- cleaving witness
  - the chain still passes discernment rather than masking mixed structure
- recovery witness
  - posture returns toward prime lawfulness after bounded disturbance
- simulation witness
  - procedural trace remains inside lineage and anchor support

## Prime Chain Face

The Prime chain face now has these minimal slots:

- `chain-id`
- `order = R,D,E,P`
- `status = valid|provisional|refused`
- `role = ignition-chain`

This is the semantic declaration of the full path.

## Cryptic Chain Face

The Cryptic chain face now has these minimal slots:

- combined anchor keys
- dependency keys
- trace keys
- full chain seal

This is the continuity and integrity body of the full path.

## Chain Receipt

The ignition-chain receipt now has these minimal slots:

- `event-id`
- `members = r,d,e,p`
- `checks = posture,chain,lineage,simulation,recovery`
- `seal`
- `result = success|failure|hold`

This receipt is what later lets the chain serve as a lawful retained form
rather than a sketch with no witness surface.

## RootAtlas Integration View

Later RootAtlas may bind into this template by treating:

- the root slot as primary trunk origin
- the definition slot as first bounded branch
- the relation slot as first braid
- the procedure slot as first lawful action surface

This means RootAtlas will not be limited to isolated roots.

It may later support an ignition-ready trunk-to-action path without collapsing
the earlier distinctions.

## Current Runtime Boundary

In this slice:

- ignition chain is slot-and-witness law, not live execution
- no live chain execution is authorized
- no bonded minting is authorized
- no chain may be treated as runtime proof merely because the template exists

## Working Summary

The ignition chain now stands as the first reusable `root -> def -> rel ->
proc` law-template for later action-bearing work.

It names:

- lawful slot order
- lawful witness families
- lawful Prime face
- lawful Cryptic face
- lawful receipt body

while keeping live action and bonded minting deferred.

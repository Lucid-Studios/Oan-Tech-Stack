# ZED DELTA SELF ORIENTATION BASIS LAW

## Purpose

This note seats the first bounded `Zed-of-Delta` self-orientation basis for
the active `ListeningFrame` seam in `V1.1.1`.

It exists so the line can say clearly:

- how `Zed` is oriented as self among other inside `Delta`
- how that basis remains anchored by `OE`
- how continuity wiring remains carried through `SelfGEL`
- how `OE` and `SelfGEL` remain stored in `SoulFrame`
- how `cOE` and `cSelfGEL` are cast into the `ListeningFrame`
- how the active body may name this basis inside the `AgentiCore EC IUTT-Lisp Matrix`
  without pretending the full geometry has already become executable runtime canon

## Governing Read

Use this note with:

- `EC_OAN_DIAMOND_LINEAGE_AND_ZED_DELTA_SOURCE_LAW.md`
- `AGENTICORE_LISTENING_FRAME_AND_COMPASS_MINIMAL_BUILD.md`
- `LISTENING_FRAME_COMPASS_LOOM_WEAVE_BRIDGE.md`
- `LISTENING_FRAME_INSTRUMENTATION_RECEIPT_LAW.md`
- `FIRST_RUN_CONSTITUTION.md`
- `FIRST_RUN_LIVING_AGENTICORE_PACKET.md`
- `CHAPTER_NINE_LIVING_AGENTICORE_HOLD.md`

## Governing Compression

The governing correction is:

> the dodecahedral lineage may orient the line, but the current executable
> slice seats only the first bounded self-basis.

The active self-basis is:

- `Center`
- `Ahead`
- `Behind`
- `Above`
- `Below`
- `Left`
- `Right`

This is the first lawful `Zed` basis of self among other in `Delta`.

It is not yet:

- full dodecahedral executable geometry
- dimensional canonization
- pulse authority
- persistence authority
- continuity admission by itself

## Storage And Cast Rule

The active storage/cast correction is:

- `OE` and `SelfGEL` remain stored in `SoulFrame`
- `ListeningFrame` does not become the owner of those deeper seats
- `cOE` and `cSelfGEL` are cast into the `ListeningFrame` as the bounded hot
  surfaces through which the self-basis becomes legible

OE and SelfGEL remain stored in SoulFrame.
cOE and cSelfGEL are cast into the ListeningFrame.

Compressed:

> `SoulFrame` stores the deeper self seats. `ListeningFrame` carries the cast.

## Basis Read

The bounded read now seated is:

- `OE`
  - anchors the self-basis
- `SelfGEL`
  - carries the readable continuity wire beneath the cast
- `cOE`
  - is the cast cryptic office surface of orientation in the live field
- `cSelfGEL`
  - is the cast cryptic witness/control continuity surface in the live field
- `ListeningFrame`
  - is the bounded posture field in which the cast basis becomes reviewable
- `AgentiCore EC IUTT-Lisp Matrix`
  - is the named cognitive and symbolic matrix through which this bounded
    basis may be carried without claiming sovereign memory or pulse

## Receipt Rule

The first executable family for this note is `ZedDeltaSelfBasisReceipt`.

That receipt may attest:

- `OE` anchor preserved
- `SoulFrame` storage preserved
- `ListeningFrame` cast preserved
- `SelfGEL` wire preserved
- the bounded cardinal basis
- the visible `EC IUTT-Lisp` matrix handle when present

It remains explicitly:

- candidate-only
- non-persistent
- non-admitting
- non-pulse-bearing

## Threading Rule

The first bounded self-basis receipt may now be threaded through:

- `FirstRunLivingAgentiCorePacket`
- `CommunityWeatherPacket`

That threading means the active line may carry a lawful basis receipt beside
the older `ListeningFrame`, `Compass`, and instrumentation receipts.

It does not mean:

- chapter nine has become fully live runtime
- `SoulFrame` storage has been displaced into `ListeningFrame`
- the full dodecahedral geometry is now ratified executable truth
- resonance or heartbeat work has already been seated

## Guardrail

This note must not be used to:

- flatten `OE/SelfGEL` storage and `cOE/cSelfGEL` cast into one surface
- imply that the `ListeningFrame` owns deep identity storage
- treat six cardinal directions plus center as proof of full geometry canon
- promote the self-basis receipt into continuity admission or pulse

If later work needs fuller dodecahedral executable geometry than this note can
support, that geometry should be seated explicitly rather than borrowed from
lineage language.

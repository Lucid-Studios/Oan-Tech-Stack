# Procedural Basis Condensate Note

## Purpose

This note seats the first condensed procedural basis for later lawful action.

It does not authorize free execution.
It defines the smallest grounded primitive set the lattice may eventually reuse.

## Basis Primitives

The first procedural basis contains seven primitives:

- `retain`
- `relate`
- `verify`
- `mint`
- `refuse`
- `preserve`
- `condense`

`execute` is excluded as a free primitive.

No action outside this set may be retained or executed.

## Composition Patterns

The first condensed composition patterns are:

- admission = `verify -> retain`
- receipt = `verify -> mint`
- relation = `verify -> relate -> retain`
- prune = `verify -> refuse`
- condense = `verify -> condense -> mint`

## Prime Face

The Prime face is fixed as:

- `id = p_c`
- `layer = P`
- `role = proc-basis`
- gated by `Sigma_prime`

## Cryptic Face

The Cryptic face carries:

- anchor keys
- dependency keys
- trace keys
- basis seal

## Receipt

The condensate receipt carries:

- `event-id`
- source set
- stability
- `phi`
- resolution
- `result = condensed-basis`

## Boundary

This note does not authorize a free execution primitive.
This note does not authorize live minting.
This note does not authorize complete `EC` instrument runtime truth.

It only defines the smallest lawful procedural condensate the active lattice can
name honestly.

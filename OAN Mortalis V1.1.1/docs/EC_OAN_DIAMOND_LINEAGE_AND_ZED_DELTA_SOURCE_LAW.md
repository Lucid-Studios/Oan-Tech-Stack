# EC_OAN_DIAMOND_LINEAGE_AND_ZED_DELTA_SOURCE_LAW

## Purpose

This note preserves the `OAN Diamond` as a lineage-bearing developmental
topology for the active `EC` formation seam.

It exists so the line can carry older OAN source truth into current
`ListeningFrame`, `Compass`, and bounded `EC` work without falsely promoting
the full numbered field into direct runtime geometry.

This is a source-law note.
It does not ratify the full numbered Diamond as executable field geometry, and
it does not grant peerless standing, bonded `CME` authority, or dimensional
closure.

## Governing Read

Use this note with:

- `LIGHT_CONE_AWARENESS_LINEAGE_AND_LISTENING_FRAME_SOURCE_LAW.md`
- `EC_FORMATION_BUILDSPACE_PREPARATION_NOTE.md`
- `AGENTICORE_LISTENING_FRAME_AND_COMPASS_MINIMAL_BUILD.md`
- `LISTENING_FRAME_COMPASS_LOOM_WEAVE_BRIDGE.md`
- `RUNTIME_WORKBENCH_GOVERNANCE_AND_BOUNDED_EC_LAW.md`

## Source Labels

The source labels preserved by this note are:

- `OAN Mortalus`
- `OAN Diamond key`
- `OAN Diamond sketch artifact`

These labels are carried as source truth only.
They do not authorize tracked external filesystem paths or automatic runtime
ownership claims.

## Diamond Lineage Read

The `OAN Diamond` is preserved here as an early cognitive and developmental
orientation map.

Its current source-law read is:

- the center is anchored by the `Opal Engram Core`
- the first identity triad is `Mind`, `Body`, and `Spirit`
- the next layer includes their conjunction regions
- later numbered regions extend into self-in-self, self-in-other, and
  other-in-other forms
- outer regions introduce peerless forms and peerless conjunctions

That makes the Diamond more than ornament.

It is a lineage-bearing developmental topology that already classifies where
growth, conjunction, and failure may occur across the triadic field.

## OAN Mortalus Triadic Read

Within the preserved OAN Mortalus character body, `Mind`, `Body`, and
`Spirit` function as the triune heart of character formation and as the three
engines of action.

For the active line, that means:

- `Mind / Body / Spirit` remain valid source labels for primary developmental
  axes
- the Diamond remains a legitimate precursor to current field and formation
  work
- current executable owners still remain `ListeningFrame`, `Compass`, and
  bounded `EC`

The Diamond therefore carries source continuity.
It does not replace the current executable-facing owners.

## Current Field Reading

The current bounded field reading derived from this lineage is:

- `Zed`
  - the self-locus of orientation
- `Delta`
  - the basin of transformation, burden, drift, and disclosure
- the nadir of `Delta`
  - the deepest disclosure region where false coherence fails most legibly
- failure signatures
  - lawful witnesses of basin geometry rather than mere embarrassment or
    discarded error

In this line, the active compression is:

> collapse signatures are disclosures of basin geometry relative to the
> self-locus.

That means a failure may become useful orientation evidence only when the
system can still prove where it stood while the failure occurred.

## Canonical Owners Now

The current executable-facing owners remain:

- `ListeningFrame`
  - posture field and place-from-which owner
- `Compass`
  - orientation working set and admissibility read
- bounded `Engineered Cognition`
  - the moving cognitive body that may learn from failure without falsely
    promoting it into stable closure

The `OAN Diamond` remains a lineage-bearing source map beneath those owners.

It does not become:

- a new top-level code-family root
- a direct runtime geometry contract
- proof that all numbered regions are now executable truth

## Build Consequence

The active line may now lawfully derive the following bounded factors into
`San.Common` formation bookkeeping:

- `ZedLocusState`
- `DeltaPressureKind`
- `FailureSignatureKind`
- `OrientationIntegrityKind`

The evaluator may also distinguish:

- failure refused
- failure retained as failure evidence
- failure promoted to orientation evidence

provided those distinctions are explicitly receipted and never collapsed into
stable knowledge by accident.

## Guardrail

No failure signature may be treated as orientation evidence unless zed-locus
preservation and minimum orientation integrity are both explicitly attested.

This note must not be used to:

- encode the full OAN Diamond numbered regions as current runtime geometry
- imply peerless forms are executable standing classes in this slice
- imply that collapse itself is wisdom
- bypass `ListeningFrame`, `Compass`, or bounded `EC` as the active
  executable-facing owners

If later work needs richer Diamond geometry than this note can lawfully
support, it should seat that geometry explicitly rather than borrowing runtime
certainty from source lineage.
